# dotfiles

Useful dotfiles.
Run
`./initialize.sh`
to start using them.
This will link
`~/.config`
to the repository,
as well as configuring
your bash login shell
with
`virtualenvwrapper`
and
`starship`
support.

